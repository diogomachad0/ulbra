#include <math.h>
#include <stdio.h>
#include <string.h>

int main (){

  int vetor[], numero, cont, posicao = 0;

  while(posicao<10){
    scanf("%d", numero);

    if(posicao==0){
    vetor[posicao]=numero;
    print("%d\n", vetor[i]);
    posicao++;
    }

  }
  
  }
